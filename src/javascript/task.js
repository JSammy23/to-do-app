const newTask = (name) => {
    let note = ""
    let dueDate = ''
    let completed = false
    let priority = 'normal';

    
    
    return {name, note, dueDate, completed, priority}

}

export default newTask